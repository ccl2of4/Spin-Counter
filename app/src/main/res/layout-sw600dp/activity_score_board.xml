<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:id="@+id/scoreboard"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="groupn.spin_counter.ScoreBoardActivity"
    android:background="@android:color/holo_blue_dark"
    android:tag="tablet_screen">

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scrollbars="none"
        android:layout_weight="1">
        <TableLayout android:id="@+id/table_layout"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:stretchColumns="*">

            <TableRow>

                <TextView android:text="@string/score_board_name" />

                <TextView android:text="@string/score_board_most_spins" />

                <TextView android:text="@string/score_board_games_played" />

                <TextView android:text="@string/score_board_games_won" />

                <TextView android:text="@string/score_board_games_lost" />

                <TextView android:text="@string/score_board_games_tied" />

            </TableRow>

        </TableLayout>
    </ScrollView>

</RelativeLayout>
